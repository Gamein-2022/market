name: build market

on:
  push:
    branches: [ "master" ]

jobs:
  build-market:
    runs-on: [self-hosted, linux]
    steps:
      - run: |
              whoami
              #docker ps
              #ls -lah /home
